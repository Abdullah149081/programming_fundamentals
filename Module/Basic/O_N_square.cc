#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m;

    cin >> n >> m;

    // if nested loop
    for (size_t i = 0; i < n; i++) // O(N*N)
    {
        for (size_t j = 0; j < m; j++)
        {
            cout << "Abdullah" << endl;
        }
    }

    return 0;
}
